name: 'Say hello'

on:
  workflow_dispatch:


jobs:
  run-my-action:
    name: checks if this action works
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: ./
        with: 
          name: 'Driesie'
